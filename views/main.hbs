{{#if register}}
    {{>register}}
{{else if registerAddress}}
    {{>registerAddress}}
{{else if login}}
    {{>login}}
{{else if home}}
    {{>home}}
{{else if logout}}
    {{>logout}}
{{else if meeting}}
    {{>meeting}}
{{/if}}